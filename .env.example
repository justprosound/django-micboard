# Django Micboard - Environment Configuration Template
# 
# Copy this file to .env.local and fill in with your actual values.
# NEVER commit .env.local to git!
# 
# This file should remain in the repository for reference by developers.
# Actual secrets should be provided via environment variables or secure vaults.

# ============================================================================
# DJANGO CONFIGURATION
# ============================================================================

# Django environment: development, staging, or production
DJANGO_ENV=development

# Django secret key (generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
DJANGO_SECRET_KEY=your-secret-key-here-replace-with-actual-value

# Allowed hosts for Django
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database configuration
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Debug mode (NEVER set to True in production!)
DEBUG=True

# ============================================================================
# SHURE INTEGRATION
# ============================================================================

# Shure System API Configuration
# These are typically configured in the Shure admin panel

# Shure API Base URL (local Shure System API)
SHURE_API_BASE_URL=http://localhost:8000

# Shure API Health Check Endpoint
SHURE_API_HEALTH_CHECK_URL=http://localhost:8000/api/v1/devices

# Shure API Timeout (seconds)
SHURE_API_TIMEOUT=10

# Shure API Request Rate Limit (requests per minute)
SHURE_API_RATE_LIMIT=120

# ============================================================================
# SHURE SYSTEM API CONNECTION (for VPN-based device population)
# ============================================================================

# List of Shure device IP addresses on VPN (comma-separated)
# Example: 172.21.1.100,172.21.1.101,172.21.1.102
SHURE_DEVICE_IPS=

# Shure device discovery timeout (seconds)
SHURE_DEVICE_DISCOVERY_TIMEOUT=5

# Verify SSL certificates when connecting to Shure devices
SHURE_DEVICE_VERIFY_SSL=false

# ============================================================================
# DJANGO CHANNELS & WEBSOCKET
# ============================================================================

# Channel Layer Backend
CHANNEL_LAYERS_BACKEND=channels.layers.InMemoryChannelLayer

# ASGI Server (daphne)
ASGI_HOST=0.0.0.0
ASGI_PORT=8000

# ============================================================================
# LOGGING & MONITORING
# ============================================================================

# Logging Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# Enable SQL Query Logging (development only)
DEBUG_SQL=false

# ============================================================================
# POLLING & BACKGROUND TASKS
# ============================================================================

# Device polling interval (seconds)
POLLING_INTERVAL=5

# Device health check interval (seconds)
HEALTH_CHECK_INTERVAL=30

# Offline device timeout (seconds) - time before marking device as offline
OFFLINE_TIMEOUT=60

# ============================================================================
# OPTIONAL: DJANGO-Q TASK QUEUE (if using async tasks)
# ============================================================================

# Task Queue Broker (redis, disque, or ORM)
TASK_QUEUE_BROKER=orm

# Redis URL (if using Redis broker)
REDIS_URL=redis://localhost:6379

# Task Queue Cluster Sync Interval (seconds)
TASK_QUEUE_SYNC_INTERVAL=1

# ============================================================================
# OPTIONAL: MONITORING & ALERTING
# ============================================================================

# Alert Threshold - Battery Level (%)
ALERT_BATTERY_THRESHOLD=10

# Alert Threshold - Frequency Interference Level (%)
ALERT_INTERFERENCE_THRESHOLD=75

# Alert Threshold - Transmitter Temperature (°C)
ALERT_TEMPERATURE_THRESHOLD=55

# ============================================================================
# OPTIONAL: EXTERNAL INTEGRATIONS
# ============================================================================

# Sennheiser Integration (if supporting multiple manufacturers)
SENNHEISER_API_ENABLED=false
SENNHEISER_API_BASE_URL=

# Wireless Monitoring System Integration
WMS_INTEGRATION_ENABLED=false
WMS_API_ENDPOINT=

# ============================================================================
# SECURITY & AUTHENTICATION
# ============================================================================

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Session Cookie Security
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_HTTPONLY=true

# CSRF Settings
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://localhost:3000

# ============================================================================
# DOCUMENTATION NOTES
# ============================================================================
#
# For local development:
#   1. Copy this file: cp .env.example .env.local
#   2. Edit .env.local with your local values
#   3. Load it: source .env.local (or use python-dotenv)
#   4. .env.local is in .gitignore and will NOT be committed
#
# For VPN device population:
#   1. Set SHURE_DEVICE_IPS to your VPN device addresses
#   2. Run: python scripts/device_discovery.py discover --env
#   3. Check device_manifest.json (will be created locally, not committed)
#   4. Run: python scripts/device_discovery.py populate
#
# For Docker deployment:
#   Pass these as environment variables when running the container:
#   docker run -e DJANGO_SECRET_KEY=your-key -e SHURE_DEVICE_IPS=... micboard:latest
#
# For Production:
#   - Use a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
#   - Set all values via environment variables
#   - NEVER commit any real secrets to git
#   - Run with DEBUG=False
#   - Use proper SSL certificates (VERIFY_SSL=true)
#

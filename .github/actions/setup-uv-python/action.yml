---
name: Set up uv and Python
description: Install the repository-pinned uv release and provision one Python runtime.

inputs:
  python-version:
    description: Python version understood by uv.
    required: false
    default: "3.13"
  enable-cache:
    description: Whether setup-uv should restore and save its dependency cache.
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d  # v10.0.1
      with:
        version: "0.12.9" # renovate: datasource=pypi depName=uv
        enable-cache: ${{ inputs.enable-cache }}

    - name: Set up Python
      shell: bash
      env:
        PYTHON_VERSION: ${{ inputs.python-version }}
      run: |
        command -v uv >/dev/null 2>&1 || {
          echo "error: uv is required" >&2
          exit 1
        }
        uv --version >/dev/null
        uv python install "$PYTHON_VERSION"
